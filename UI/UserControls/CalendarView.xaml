<UserControl x:Class="UI.UserControls.CalendarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UI.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="45"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Height="45" Margin="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
                <ColumnDefinition Width="2*"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Button Content="Лист" Grid.Column="0" Height="25" Click="ListButton_Click" Style="{StaticResource RoundButtonStyleCalendar}"></Button>
            <Button Content="День" Grid.Column="1" Height="25" Click="DayButton_Click" Style="{StaticResource RoundButtonStyleCalendar}"></Button>
            <Button Content="Неделя" Grid.Column="2" Height="25" Click="WeekButton_Click" Style="{StaticResource RoundButtonStyleCalendar}"></Button>
            <Button Content="Месяц" Grid.Column="3" Height="25" Click="MonthButton_Click" Style="{StaticResource RoundButtonStyleCalendar}"></Button>
            <Button Content="Создать событие" Grid.Column="4" Height="25" Click="CreateNewEvent_Click" Style="{StaticResource RoundButtonStyleCalendar}"></Button>
        </Grid>


        <ContentControl x:Name="CalendarContentArea" Grid.Row="1" Background="AliceBlue"></ContentControl>
    </Grid>
</UserControl>
